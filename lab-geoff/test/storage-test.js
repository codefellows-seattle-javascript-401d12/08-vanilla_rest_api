'use strict';

const expect  = require('chai').expect;

// Note that this test is similar to our API test,
// because our API is basically a CRUD REST API.

// However, since we probably aren't going to use
// file storage in the long run, perhaps we should
// just rely on our existing test.
describe('Storage', function() {
  var player = null;

  describe('#createItem', function() {
  });

  describe('#fetchItem', function() {
  });

  describe('#deleteItem', function() {
  });
});
